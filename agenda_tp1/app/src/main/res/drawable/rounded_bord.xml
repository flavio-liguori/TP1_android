<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/backgroundcolor" /> <!-- Couleur de fond -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Contour blanc -->
    <corners android:radius="16dp" /> <!-- Coins arrondis -->
</shape>
